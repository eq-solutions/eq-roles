/* GENERATED by build.mjs from roles/model.json — do not edit. Run `npm run build`. */
/* @eq-solutions/roles/service v2.7.2 — service-module permission slice.
 * Import from "@eq-solutions/roles/service" for typed service.* perms only.
 * Full 60-perm matrix lives in the root "@eq-solutions/roles" entry. */

/** EqRole — inlined so this module file has zero imports. */
export type EqRole = 'manager' | 'supervisor' | 'employee' | 'apprentice' | 'labour_hire' | 'subcontractor';

export type ServicePermKey = 'service.view' | 'service.view_commercials' | 'service.create' | 'service.close' | 'service.reopen' | 'service.record_tests' | 'service.do_work';
export const SERVICE_PERM_KEYS: readonly ServicePermKey[] = ['service.view', 'service.view_commercials', 'service.create', 'service.close', 'service.reopen', 'service.record_tests', 'service.do_work'];
export const SERVICE_PERMISSIONS = [{"key":"service.view","module":"service","label":"View maintenance","description":"See work orders, faults, and scheduled maintenance.","roles":["manager","supervisor","employee","apprentice"]},{"key":"service.view_commercials","module":"service","label":"See job pricing","description":"See what a job is worth — its price and contract value. Kept separate from viewing maintenance so people can do the work without seeing the money.","roles":["manager","supervisor"]},{"key":"service.create","module":"service","label":"Raise work orders","description":"Raise a new work order or report a fault.","roles":["manager","supervisor"]},{"key":"service.close","module":"service","label":"Close work orders","description":"Mark a work order as finished.","roles":["manager","supervisor"]},{"key":"service.reopen","module":"service","label":"Reopen a work order","description":"Reopen a work order that was already closed. Kept to a small group so the activity log stays meaningful.","roles":["manager","supervisor"]},{"key":"service.record_tests","module":"service","label":"Log calibration results","description":"Record calibration or test results against a piece of equipment.","roles":["manager","supervisor"]},{"key":"service.do_work","module":"service","label":"Carry out and record work","description":"Run test procedures and record their results, and raise an on-site check, in EQ Service.","roles":["manager","supervisor","employee"]}] as const;

/** Per-role grants within the service module. */
export const SERVICE_MATRIX: Record<EqRole, readonly ServicePermKey[]> = {
  "manager": [
    "service.view",
    "service.view_commercials",
    "service.create",
    "service.close",
    "service.reopen",
    "service.record_tests",
    "service.do_work"
  ],
  "supervisor": [
    "service.view",
    "service.view_commercials",
    "service.create",
    "service.close",
    "service.reopen",
    "service.record_tests",
    "service.do_work"
  ],
  "employee": [
    "service.view",
    "service.do_work"
  ],
  "apprentice": [
    "service.view"
  ],
  "labour_hire": [],
  "subcontractor": []
};

const _SERVICE_SETS: Record<EqRole, Set<ServicePermKey>> = Object.fromEntries(
  (Object.keys(SERVICE_MATRIX) as EqRole[]).map((r) => [r, new Set(SERVICE_MATRIX[r])]),
) as Record<EqRole, Set<ServicePermKey>>;

export function serviceCan(role: EqRole, perm: ServicePermKey, opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return _SERVICE_SETS[role]?.has(perm) ?? false;
}

export function permissionsForService(role: EqRole, opts?: { isPlatformAdmin?: boolean }): readonly ServicePermKey[] {
  if (opts?.isPlatformAdmin) return SERVICE_PERM_KEYS.slice();
  return SERVICE_MATRIX[role] ?? [];
}

export function serviceCanAny(role: EqRole, perms: readonly ServicePermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.some((p) => _SERVICE_SETS[role]?.has(p));
}

export function serviceCanAll(role: EqRole, perms: readonly ServicePermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.every((p) => _SERVICE_SETS[role]?.has(p));
}
