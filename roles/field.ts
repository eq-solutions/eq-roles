/* GENERATED by build.mjs from roles/model.json — do not edit. Run `npm run build`. */
/* @eq-solutions/roles/field v2.7.4 — field-module permission slice.
 * Import from "@eq-solutions/roles/field" for typed field.* perms only.
 * Full 61-perm matrix lives in the root "@eq-solutions/roles" entry. */

/** EqRole — inlined so this module file has zero imports. */
export type EqRole = 'manager' | 'supervisor' | 'employee' | 'apprentice' | 'labour_hire' | 'subcontractor';

export type FieldPermKey = 'field.view' | 'field.view_hours' | 'field.view_licences' | 'field.view_rates' | 'field.dispatch' | 'field.manage_roster' | 'field.manage_licences' | 'field.manage_labour_hire' | 'field.manage_people';
export const FIELD_PERM_KEYS: readonly FieldPermKey[] = ['field.view', 'field.view_hours', 'field.view_licences', 'field.view_rates', 'field.dispatch', 'field.manage_roster', 'field.manage_licences', 'field.manage_labour_hire', 'field.manage_people'];
export const FIELD_PERMISSIONS = [{"key":"field.view","module":"field","label":"View field information","description":"See staff, licences, availability, and job assignments in EQ Field.","roles":["manager","supervisor","employee","apprentice","labour_hire","subcontractor"]},{"key":"field.view_hours","module":"field","label":"See timesheets & hours","description":"See the hours workers have logged. Kept separate because hours affect pay.","roles":["manager","supervisor"]},{"key":"field.view_licences","module":"field","label":"See worker licences","description":"See workers’ licences and tickets, and whether they are current.","roles":["manager","supervisor"]},{"key":"field.view_rates","module":"field","label":"See charge rates","description":"See what labour-hire workers are charged out at.","roles":["manager","supervisor"],"deprecated":"Labour-hire rates are actually controlled by ops.view_rates, in both the app and the database — this key is never checked. Kept only so nobody's existing access breaks; do not grant to anyone new. Use ops.view_rates instead."},{"key":"field.dispatch","module":"field","label":"Dispatch staff","description":"Send staff to jobs and shifts.","roles":["manager","supervisor"]},{"key":"field.manage_roster","module":"field","label":"Build & edit the roster","description":"Build and change the crew roster. Separate from day-to-day dispatch because it sets the plan rather than following it.","roles":["manager","supervisor"]},{"key":"field.manage_licences","module":"field","label":"Manage worker licences","description":"Add a worker’s licence or confirm you have sighted it. This is a sign-off — it records that someone checked.","roles":["manager","supervisor"]},{"key":"field.manage_labour_hire","module":"field","label":"Manage labour-hire companies","description":"Add or change the labour-hire companies you use and what they charge.","roles":["manager","supervisor"]},{"key":"field.manage_people","module":"field","label":"Add / remove / edit workers","description":"Add a worker, change their details, remove or restore them, and reset their PIN.","roles":["manager","supervisor"]}] as const;

/** Per-role grants within the field module. */
export const FIELD_MATRIX: Record<EqRole, readonly FieldPermKey[]> = {
  "manager": [
    "field.view",
    "field.view_hours",
    "field.view_licences",
    "field.view_rates",
    "field.dispatch",
    "field.manage_roster",
    "field.manage_licences",
    "field.manage_labour_hire",
    "field.manage_people"
  ],
  "supervisor": [
    "field.view",
    "field.view_hours",
    "field.view_licences",
    "field.view_rates",
    "field.dispatch",
    "field.manage_roster",
    "field.manage_licences",
    "field.manage_labour_hire",
    "field.manage_people"
  ],
  "employee": [
    "field.view"
  ],
  "apprentice": [
    "field.view"
  ],
  "labour_hire": [
    "field.view"
  ],
  "subcontractor": [
    "field.view"
  ]
};

const _FIELD_SETS: Record<EqRole, Set<FieldPermKey>> = Object.fromEntries(
  (Object.keys(FIELD_MATRIX) as EqRole[]).map((r) => [r, new Set(FIELD_MATRIX[r])]),
) as Record<EqRole, Set<FieldPermKey>>;

export function fieldCan(role: EqRole, perm: FieldPermKey, opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return _FIELD_SETS[role]?.has(perm) ?? false;
}

export function permissionsForField(role: EqRole, opts?: { isPlatformAdmin?: boolean }): readonly FieldPermKey[] {
  if (opts?.isPlatformAdmin) return FIELD_PERM_KEYS.slice();
  return FIELD_MATRIX[role] ?? [];
}

export function fieldCanAny(role: EqRole, perms: readonly FieldPermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.some((p) => _FIELD_SETS[role]?.has(p));
}

export function fieldCanAll(role: EqRole, perms: readonly FieldPermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.every((p) => _FIELD_SETS[role]?.has(p));
}
