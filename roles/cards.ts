/* GENERATED by build.mjs from roles/model.json — do not edit. Run `npm run build`. */
/* @eq-solutions/roles/cards v2.4.0 — cards-module permission slice.
 * Import from "@eq-solutions/roles/cards" for typed cards.* perms only.
 * Full 30-perm matrix lives in the root "@eq-solutions/roles" entry. */

/** EqRole — inlined so this module file has zero imports. */
export type EqRole = 'manager' | 'supervisor' | 'employee' | 'apprentice' | 'labour_hire' | 'subcontractor';

export type CardsPermKey = 'cards.view' | 'cards.onboard';
export const CARDS_PERM_KEYS: readonly CardsPermKey[] = ['cards.view', 'cards.onboard'];
export const CARDS_PERMISSIONS = [{"key":"cards.view","module":"cards","label":"Open Cards","description":"Access the Cards onboarding surface.","roles":["manager","supervisor","employee","apprentice"]},{"key":"cards.onboard","module":"cards","label":"Submit onboarding","description":"Submit or manage an onboarding card.","roles":["manager","supervisor"]}] as const;

/** Per-role grants within the cards module. */
export const CARDS_MATRIX: Record<EqRole, readonly CardsPermKey[]> = {
  "manager": [
    "cards.view",
    "cards.onboard"
  ],
  "supervisor": [
    "cards.view",
    "cards.onboard"
  ],
  "employee": [
    "cards.view"
  ],
  "apprentice": [
    "cards.view"
  ],
  "labour_hire": [],
  "subcontractor": []
};

const _CARDS_SETS: Record<EqRole, Set<CardsPermKey>> = Object.fromEntries(
  (Object.keys(CARDS_MATRIX) as EqRole[]).map((r) => [r, new Set(CARDS_MATRIX[r])]),
) as Record<EqRole, Set<CardsPermKey>>;

export function cardsCan(role: EqRole, perm: CardsPermKey, opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return _CARDS_SETS[role]?.has(perm) ?? false;
}

export function permissionsForCards(role: EqRole, opts?: { isPlatformAdmin?: boolean }): readonly CardsPermKey[] {
  if (opts?.isPlatformAdmin) return CARDS_PERM_KEYS.slice();
  return CARDS_MATRIX[role] ?? [];
}

export function cardsCanAny(role: EqRole, perms: readonly CardsPermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.some((p) => _CARDS_SETS[role]?.has(p));
}

export function cardsCanAll(role: EqRole, perms: readonly CardsPermKey[], opts?: { isPlatformAdmin?: boolean }): boolean {
  if (opts?.isPlatformAdmin) return true;
  return perms.every((p) => _CARDS_SETS[role]?.has(p));
}
